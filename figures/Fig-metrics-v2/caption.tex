\caption{Definition of DI,SC and comparison to other metrics.
\textbf{(A)} DI and SC are defined from SR (signal ratio matrix) and DM (differentiation matrix). Bar on top of SR in the SC definition indicates taking average. 
\textbf{(B)} Various metrics are calculated for simple cases.
\textbf{(C)} Summary of characteristics of the metrics.
\textbf{(D)} DI, SC and ANOVA p-values calculated from the current dataset. Red dots indicate genes with high value for the metric not shown in the respective scatter plot. 
}